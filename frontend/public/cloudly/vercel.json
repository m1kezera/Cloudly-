{
  "version": 2,
  "builds": [
    { "src": "frontend/public/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "frontend/public/$1" }
  ]
}
